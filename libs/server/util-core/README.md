# server-util-core

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build server-util-core` to build the library.

## Running unit tests

Run `nx test server-util-core` to execute the unit tests via [Vitest](https://vitest.dev/).
